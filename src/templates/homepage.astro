---
import { contact, company, contact_person } from "../data/site-data.json";
import berita from "../data/berita.json";
import galery from "../data/galery.json";
import HomepageMainContent from "./sections/homepage-main-content.astro";

const page = {
	title: `${company.name} Pengurus ${company.shortAddress}`,
	meta: {
		name: "ponpes kanzul ulum kesambi, pondok pesantren kanzul ulum, pondok pesantren kanzul ulum kesambi cirebon, pondok pesantren kanzul ulum kesambi, pondok pesantren kanzul ulum cirebon, pondok pesantren di cirebon, ponpes cirebon, pengurus pondok pesantren kanzul ulum",
		description: `${company.shortAddress} Hub. ${contact.phone}`,
	},
};

import ColumnLayout from "./layouts/column.astro";
const { frontmatter } = Astro.props;
---

<ColumnLayout page={page} contact={contact} cp={contact_person} company={company}>
  <HomepageMainContent berita={berita} galery={galery}/>
</ColumnLayout>
 